# Acquisition run — 2026-08-16 20:10 IST

## Scope of this run

1. Added provenance tracking to the pipeline (`pipeline_lib/manifest.py`):
   per-source `pdf_raw/<source>/_manifest.jsonl` ledger (one JSON line per
   file: filename, source_url, fetched_at, gazette_id, year, http_status,
   pdf_header_verified, bytes), a `_status.json` per source recording the
   last fetch outcome, and `build_corpus_manifest.py` which regenerates
   `production_pipeline/corpus_manifest.json` purely from what's on disk
   (including loose PDFs sitting directly under `pdf_raw/`, tallied under a
   `_root_untracked` pseudo-source so the total always matches disk).
2. Wired manifest logging + skip-already-downloaded into `bulk_fetch_egazette.py`
   (source: `egazette_central`) and `fetch_mha_paginated.py` (source:
   `mha_central`). These two were chosen because they're the scripts that do
   a real, reproducible fetch (ID-range sweep for egazette, paginated listing
   crawl for MHA) rather than one-off JS-click probes.
   Skip logic checks two things in order: (a) filename already verified in
   the manifest, or (b) file already exists on disk at valid size (a
   pre-manifest legacy file) — in both cases the file is left untouched and
   never re-downloaded/overwritten. Legacy files matched by (b) simply don't
   get a provenance record retroactively, since we have no way to know their
   original source URL was actually what the current script would guess.
3. Ran `bulk_fetch_egazette.py` (egazette_central), then `fetch_mha_paginated.py`
   (mha_central).

## Source fetched: egazette_central

- **Method**: ID-range sweep against `https://egazette.gov.in/WriteReadData/2026/<id>.pdf`,
  range 275301–275491 (same range as the pre-existing script default).
- All 191 IDs in range were already present on disk before this run, with no
  provenance record (manifest didn't exist yet). **Note on this specific
  run**: the first version of the updated script re-requested and overwrote
  all 191 files in order to backfill provenance — every file verified
  byte-identical against the live server (HTTP 200, `%PDF` header), so no
  content was lost, but this was a bug (skip logic didn't check "exists on
  disk" for pre-manifest files, only "already in manifest"). Fixed
  immediately after by adding an on-disk existence+size check, and confirmed
  idempotent. **No files were overwritten by any run after the fix.**
- Probed for expansion beyond the known range (IDs 275492–275519 above, and
  275250–275300 below): all returned HTTP 404. The current contiguous block
  appears to be the full set of live IDs in the `2026` folder near this
  range; no new IDs were found to add.
- **Barrier**: this is still a guessed ID-range sweep, not a crawl of a real
  gazette listing/search page — years 1958–2025 and any other ID clusters in
  2026 are completely untried. `egazette.gov.in`'s search UI is JS-driven and
  wasn't crawled in this run.
- **Status written**: `partial` — "Only the 2026 WriteReadData folder has
  been swept (IDs 275301-275491). Years 1958-2025 and any IDs outside this
  range are untried."
- **New count added**: 0 net-new PDFs (191 → 191); all 191 now have verified
  provenance records for the first time.

## Source fetched: mha_central

- **Method**: paginated listing crawl over `https://www.mha.gov.in/en/notifications/circular`
  and `.../archive-circular` (`?page=0..34` each, stopping a listing once a
  page returns zero new PDF links).
- Walked both listings end-to-end: `notifications/circular` stopped
  yielding new links after page 1, `archive-circular` after page 13.
  296 unique PDF links discovered total.
- 286 of those matched pre-existing on-disk files (left untouched, not
  re-downloaded). **10 new PDFs downloaded and verified**, each with a
  manifest entry recording its exact `mha.gov.in` source URL.
- **Barrier**: only two of MHA's listing sections were crawled. Other
  sections (`common-circulars`, `document-reports`, press releases, orders)
  were not touched by this script, so the source as a whole remains
  `partial` even though these two specific listings were walked exhaustively.
- **Status written**: `partial` — the two listings crawled are exhausted,
  but MHA has more sections outside this script's scope.
- **New count added**: 10 net-new PDFs (296 → 306).

## Not touched this run

`india_code` (0 files on disk) and `home_department` (1580 files, partial
per original task framing) were not fetched or wired into manifest logging
this run — out of scope for this session. `pipeline_lib/manifest.py` is
generic and ready to be wired into their fetch scripts next.

## Resulting totals (from `production_pipeline/corpus_manifest.json`, generated 2026-08-16T20:12:50+05:30)

| source | count | years_covered | status |
|---|---|---|---|
| egazette_central | 191 | [2026] | partial |
| mha_central | 306 | [] | partial |
| india_code | 0 | [] | unknown |
| home_department | 1580 | [] | unknown |
| _root_untracked (loose PDFs directly in pdf_raw/) | 105 | [] | unknown |
| **total_pdfs (all 42 source entries)** | **2846** | | |
